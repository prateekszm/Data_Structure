#include<bits/stdc++.h>
using namespace std;
int main(){
	clock_t z = clock();
	vector<char> op;
	op.push_back('k');
	// cout<<(3&1);
	cout<<(0|2);
	cout<<(2|0);
	// cout<<op.size()<<endl;
	cerr << "Run Time : " << ((double)(clock() - z) / CLOCKS_PER_SEC)<<endl;
}